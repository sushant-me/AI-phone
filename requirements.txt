# ---------------------------------------------------------------------------
# Maya — Nepanglish AI Phone Order Taker (Python backend)
# ---------------------------------------------------------------------------
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
requests>=2.28

# ASR (local speech-to-text)
faster-whisper>=1.0

# TTS engines
edge-tts>=6.1
piper-tts>=1.7
soundfile>=0.12
numpy

# ---------------------------------------------------------------------------
# Human GPU voice (OmniVoice) — install SEPARATELY, see README step 4:
#
#   pip install torch==2.11.0+cu128 torchaudio==2.11.0+cu128 torchvision==0.26.0+cu128 --extra-index-url https://download.pytorch.org/whl/cu128
#   pip install omnivoice
#   pip install "transformers==5.8.1"
# ---------------------------------------------------------------------------
